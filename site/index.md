---
title: 学习地图
---
# 学习地图

> **MyKnowledge** —— 私人学习网站。内容公开在 GitHub Pages；**你的行为数据只存在本机浏览器**（IndexedDB，实时落盘），
> 可一键导出/导入/快照复原。架构与语义见仓库 `specs/data-model.md`。

## 使用流程（每单元）

1. 打开一个单元页面（= 一次 `unit_open`，自动记录）
2. 按页面内容学习：先学 → 例句朗读 → 完成页底自检
3. 学完点"标记完成"（N5 接入后自动记录 `unit_complete`；当前阶段可先用 tracker 演示页验证数据流）
4. 定期**导出**一份 JSON 留作离线复原副本；误操作时用快照/导入回滚

## 学习进度（本机实时）

> 以下由你本机的行为数据驱动：打开单元页自动记为"打开"，点"标记完成"写入本地。数据不出浏览器。

<ProgressPanel />

## 当前赛道

| 赛道 | 说明 | 单元数 | 状态 |
|---|---|---|---|
| [英语 · 词汇核心](/english/vocab-core/unit-01-core-verbs) | 高频词 12 词/单元 + 例句 | 3 | 内容 v0，追踪 UI 待接入 (N5) |
| [英语 · 基础语法](/english/grammar-basics/unit-01-tenses) | 时态、句子成分 | 2 | 同上 |

> 进度可视化、单元勾选、复习队列等属于路线图 N5（追踪 UI），本阶段先保证：内容结构 + 数据层 + 复原验证全部就绪。

## 复原手册（三句话）

- **日常**：浏览器内快照自动留最近 5 份 → 误操作可从快照恢复
- **换机/换浏览器**：旧机导出 → 新机导入
- **灾难恢复**：清空 → 导入最近导出文件 → 状态逐字段一致（幂等，可重复执行）
